const evento = document.getElementById("evento"); const pelle = document.getElementById("pelle"); const cards = document.querySelectorAll(".card"); function filtraTutorial(){ const valoreEvento = evento.value; const valorePelle = pelle.value; cards.forEach(card => { const eventoCard = card.dataset.evento; const pelleCard = card.dataset.pelle; const matchEvento = valoreEvento === "all" || valoreEvento === eventoCard; const matchPelle = valorePelle === "all" || valorePelle === pelleCard; if(matchEvento && matchPelle){ card.style.display = "block"; } else { card.style.display = "none"; } }); } evento.addEventListener("change", filtraTutorial); pelle.addEventListener("change", filtraTutorial);
